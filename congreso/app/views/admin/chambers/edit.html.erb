<legend>
  Editing chamber
  <%= link_to 'Destroy', @chamber, method: :delete, data: { confirm: 'Are you sure?' }, :class => [:btn, "btn-danger"] %>
</legend>

<%= render 'form' %>

<%= link_to 'Back', admin_chambers_path %>
