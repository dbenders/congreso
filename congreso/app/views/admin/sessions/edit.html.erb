<legend>
  Editing session
  <%= link_to 'Destroy', @session, method: :delete, data: { confirm: 'Are you sure?' }, :class => [:btn, "btn-danger"] %>
</legend>

<%= render 'form' %>

<%= link_to 'Back', admin_sessions_path %>
